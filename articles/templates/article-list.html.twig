{% if articles %}    
    <ul class="article-list">        
        {% for article in articles %}
            <li>
                <a href="{{ path('entity.node.canonical', {'node': article.node.id}) }}">{{ article.node.label|t }}</a>
            </li>
        {% endfor %}                                
    </ul>
{% endif %}